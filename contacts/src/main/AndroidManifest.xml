<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.contacts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.main.LogoActivity"></activity>
        <activity android:name=".main.main.MainActivity" />
        <activity android:name=".main.main.MainListViewActivity" />
        <activity android:name=".main.datastorage.SharedPreferActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.datastorage.InternalStoActivity"></activity>
    </application>

</manifest>